export interface ParcelMenuProps {}

export function ParcelMenuPage(props: ParcelMenuProps): React.ReactElement {
  /*
   * Need to select parcels
   *
   * MAP
   *  - select neighborhood
   *  - select municipality
   *  - pick parcel one by one/ drag and select
   *  - draw shape
   *
   * FORMS
   *   - select neighborhood
   *   - select municipality
   *   - Enter custom filter using react-sql
   * */

  return <div />;
}
