import type { Meta, StoryObj } from "@storybook/react";
import type { {{pascalCase name}}Props } from "@wprdc/ui";
import { {{pascalCase name}} } from "@wprdc/ui";

const meta: Meta<typeof {{pascalCase name}}> = {
  component: {{pascalCase name}},
};

export default meta;

type Story = StoryObj<typeof {{pascalCase name}}>;

export const Primary: Story = {
  render: (props: {{pascalCase name}}Props) => <{{pascalCase name}} {...props}></{{pascalCase name}}>,
  name: "{{titleCase name}}",
  args: {},
};
