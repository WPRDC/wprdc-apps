/**
*
* {{ pascalCase name }}
*
* {{ description }}
*
**/
import * as React from "react";
import type { {{ pascalCase name }}Props } from "./{{ pascalCase name }}.types.ts"

export function {{ pascalCase name }}({ children }: {{ pascalCase name }}Props): React.ReactElement {
  return (
    <div>
      {children}
    </div>
  );
};