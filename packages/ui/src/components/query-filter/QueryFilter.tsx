/**
 *
 * QueryFilter
 *
 * Form used to generate filter expressions.
 *
 **/
"use client";

import * as React from "react";
import type { QueryFilterProps } from "./QueryFilter.types";
import "react-querybuilder/dist/query-builder.css";

export function QueryFilter({}: QueryFilterProps): React.ReactElement {
  return <div />;
}
